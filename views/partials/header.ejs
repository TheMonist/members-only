<nav class="navbar">
    <div class="header">
        <a href="/"> <h1>Members Only</h1></a>
    </div>
    <div class="links">
        <% if (currentUser) { %>
            <a href="/logout">Log Out</a>
        <% } else { %>
            <a href="/signup">Sign Up</a>
            <a href="/login">Log In</a>
        <% } %>
    </div>
</nav>
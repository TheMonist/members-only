<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Home</title>
        <link rel="stylesheet" href="/styles.css" />
    </head>
    <body>
        <div class="container">
            <%- include("partials/navbar") %>
            <div class="main-content">
                <% if(currentUser) { %>
                    <h1>Welcome abord, <%=currentUser.firstname%></h1>
                    <div class="message-board">
                        <div class="add-messages-container">
                            <form action="/newMessage" method="post" class="new-message-form">
                                <label for="name">Title</label>
                                <input type="text" name="title" id="title">
                                <label for="message">Message</label>
                                <textarea name="message" id="message" cols="30" rows="10" required></textarea>
                                <button type="submit" class="new-message-btn">Add New Message</button>
                            </form>
                        </div>
                    </div>
                <% ) else { %>
                    <h1>Welcome to the Ship</h1>
                    <div class="btn-container">
                        <a href="/signup" class="general-btn">Sign Up</a>
                        <a href="/login" class="general-btn">Log In</a>
                    </div>
                    <h2>You must be logged in to view topics</h2>
                <% %>
            </div>
            <%- include("partials/footer") %>
        </div> 
    </body>
</html>